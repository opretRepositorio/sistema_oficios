@model IEnumerable<OfiGest.Models.DivisionesModel>
@{
    ViewData["Title"] = "Listado de Divisiones";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var rolUsuario = Context.Session.GetString("RolUsuario");
}


<div class="container-fluid d-flex flex-column compact-container">
    <h4 class="mt-3 mb-3">Listado de Divisiones</h4>

    <div class="card shadow-sm border-0 p-4 card-adjusted">
        <div class="search-box">
            <i class="bi bi-search" style="font-size: 1.2rem;"></i>
            <input type="text" id="searchInput" placeholder="Buscar por nombre o departamento" />
        </div>

        <div class="d-flex justify-content-between align-items-center mb-2">
            <small id="contadorDivisiones" class="text-muted">Divisiones visibles: @Model.Count()</small>
        </div>

        <div class="table-wrapper mb-3">
            <table id="tablaDivisiones" class="table table-bordered table-striped align-middle mb-0">
                <thead>
                    <tr>
                        <th>Iniciales</th>
                        <th>Nombre</th>
                        <th>Departamento</th>
                        <th>Descripción</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var u in Model)
                    {
                        <tr>
                            <td>@u.Iniciales</td>
                            <td>@u.Nombre</td>
                            <td>@u.NombreDepartamento</td>
                            <td>@u.Descripcion</td>
                            <td class="text-center">
                                <div class="d-flex justify-content-center gap-2">
                                    @if (rolUsuario == "Usuario")
                                    {
                                        <button type="button" class="btn-opret-sm" title="Solo visual" onclick="mostrarAlertaVisual()">
                                            <i class="bi bi-eye"></i>
                                        </button>
                                    }
                                    else if (rolUsuario == "Administrador")
                                    {
                                        <a asp-action="Edit" asp-route-id="@u.Id" class="btn-opret-sm" title="Editar">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        <button type="button" class="btn-opretEliminar-sm" onclick="deleteItem(@u.Id)" title="Eliminar">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <div id="noResults" class="text-muted text-center mt-2" style="display:none;">
                No se encontraron divisiones que coincidan con la búsqueda.
            </div>
        </div>

    
            <div class="mt-auto">
                <a asp-action="Create" class="btn btn-azul d-flex justify-content-center align-items-center gap-2 w-100">
                    CREAR DIVISIÓN
                </a>
            </div>
    
   
    </div>
</div>

@section Scripts {

   
    <script>
         document.addEventListener("DOMContentLoaded", function () {
            const successMessage = '@Html.Raw(TempData["Success"]?.ToString())';
            const errorMessage = '@Html.Raw(TempData["Error"]?.ToString())';

            if (successMessage) {
            Swal.fire({
                  title: "¡Módulo Divisiones!",
                text: successMessage,
                icon: "success",
                timer: 2000,
                showConfirmButton: false,
                timerProgressBar: true
            });
        }
        else if (errorMessage) {
            Swal.fire({
                title: "¡Módulo Divisiones!",
                text: errorMessage,
                icon: "error",
                confirmButtonText: "Entendido",
                confirmButtonColor: "#3D6FAD"
            });
        }

         const input = document.getElementById("searchInput");
            if (input) {
                document.getElementById("searchInput").addEventListener("input", function () {
                filtrarTablaPorColumnas("searchInput", "tablaDivisiones", [1, 2]);
                });
            }
        });



        function filtrarTablaPorColumnas(inputId, tablaId, columnas) {
            const filtro = document.getElementById(inputId)?.value.toLowerCase();
            const filas = document.querySelectorAll(`#${tablaId} tbody tr`);
            let visibles = 0;

            filas.forEach(fila => {
                const coincide = columnas.some(index => {
                    const texto = fila.children[index]?.textContent.toLowerCase() || "";
                    return texto.includes(filtro);
                });
                fila.style.display = coincide ? "" : "none";
                if (coincide) visibles++;
            });

            const mensaje = document.getElementById("noResults");
            if (mensaje) {
                mensaje.style.display = visibles === 0 ? "block" : "none";
            }

            const contador = document.getElementById("contadorDivisiones");
            if (contador) {
                contador.textContent = `Divisiones visibles: ${visibles}`;
            }
        }

        function deleteItem(divisionId) {
            Swal.fire({
                title: "¿Estás seguro?",
                text: "¡Una vez eliminado, no podrás recuperar esta division!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonText: "Sí, eliminar",
                cancelButtonText: "Cancelar",
                confirmButtonColor: "#F20505",
                cancelButtonColor: "#3D6FAD"
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = '@Url.Action("Delete", "Divisiones")' + '?id=' + divisionId;
                }
            });
        }


         function mostrarAlertaVisual() {
            Swal.fire({
                title: "Solo visual",
                text: "No tienes permisos para editar o eliminar esta division.",
                 icon: "warning",
                confirmButtonText: "Entendido",
                confirmButtonColor: "#3D6FAD"
            });
        }
    </script>
}